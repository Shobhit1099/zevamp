import { Widget } from "@typeform/embed-react";

const Typeform = () => {
  return (
    <Widget
    style={{ border: "1px solid black" }} 
      id="UelgAMed"
      height={500}
      style={{ width: "65%" }}
      className="my-form"
    />
  );
};

export default Typeform;
